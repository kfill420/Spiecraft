{
  "name": "poivre",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy": "gh-pages -d dist",
    "build": "cd ./front && vite build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:front": "cd front && pnpm run dev",
    "dev:back": "cd back && pnpm run dev",
    "dev": "concurrently \"cd back && pnpm run dev\" \"cd front && pnpm run dev\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "jwt-decode": "^4.0.0"
  }
}